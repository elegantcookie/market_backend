version: "3.8"
services:
  user_service:
    build: ./user_service
    ports:
      - 10002:10002
    environment:
      - HOST=0.0.0.0
      - PORT=10002
  database:
    image: mongo:5
    ports:
      - 2717:27017

# actually doesn't work